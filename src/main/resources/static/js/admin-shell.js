(function () {
  // Deprecated: UI injection moved to Thymeleaf fragments.
  // Keep this file only as a safe placeholder for old script references.
  window.BookomPanelShell = {
    init: function () {
      return;
    }
  };
})();
